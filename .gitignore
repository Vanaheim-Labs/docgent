# build artefacts
build/
dist/
.next/

# QA renders
.qa/

# deps
node_modules/
.venv/
__pycache__/
*.pyc

# render worker staged build context (regenerate with stage.mjs)
apps/render-worker/pipeline/

# env
.env
.env.local

# os
.DS_Store

# studio
apps/studio/.env.local
apps/studio/.next/
apps/studio/node_modules/
apps/studio/.vercel
.vercel

# batch dry-run scratch space
.dryrun/

# QA render output — never commit, may contain confidential document pages
nf-extract/
nfqa/

# Documents live in per-brand stores, never in the pipeline repo:
#   Vanaheim-Labs/docforge-documents
#   north-face-investments/docforge-documents
documents/

# brands/ is a git submodule (see .gitmodules) pointing at a private repo
# of real brand configs — nothing under it is tracked directly here.

# local credentials (never commit)
.secrets/
*.token
